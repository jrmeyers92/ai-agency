---
// Hero.astro
---

<style>
  @keyframes gradientFlow {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .gradient-text {
    background: linear-gradient(135deg, #3b82f6, #8b5cf6, #3b82f6);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    background-size: 200% 200%;
    animation: gradientFlow 6s ease infinite;
  }

  .grid-pattern {
    background-image: radial-gradient(#1e293b 1px, transparent 1px);
    background-size: 24px 24px;
  }

  @keyframes slideUpFade {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-slide-up {
    opacity: 0;
    animation: slideUpFade 0.8s ease-out forwards;
  }

  .animate-slide-up-delay-1 {
    opacity: 0;
    animation: slideUpFade 0.8s ease-out 0.2s forwards;
  }

  .animate-slide-up-delay-2 {
    opacity: 0;
    animation: slideUpFade 0.8s ease-out 0.4s forwards;
  }
</style>

<div class="relative w-full min-h-screen overflow-hidden">
  <!-- Modern Gradient Background -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-neutral-900 via-neutral-800 to-neutral-900"
  >
    <!-- Animated Background Elements -->
    <div
      class="absolute top-20 left-20 w-72 h-72 bg-primary-500/10 rounded-full blur-3xl animate-float"
    >
    </div>
    <div
      class="absolute bottom-20 right-20 w-96 h-96 bg-accent-500/10 rounded-full blur-3xl animate-float"
      style="animation-delay: -3s;"
    >
    </div>
    <div
      class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-primary-600/5 rounded-full blur-3xl"
    >
    </div>
  </div>

  <!-- Subtle Grid Pattern -->
  <div
    class="absolute inset-0 opacity-[0.03]"
    style="background-image: radial-gradient(circle at 1px 1px, white 1px, transparent 0); background-size: 24px 24px;"
  >
  </div>

  <!-- Main Content -->
  <div
    class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-32 pb-20 flex flex-col lg:flex-row items-center justify-between gap-16"
  >
    <!-- Left Column - Text Content -->
    <div class="flex-1 text-center lg:text-left space-y-8">
      <!-- Badge -->
      <div
        class="animate-fade-in-up inline-flex items-center px-4 py-2 rounded-full bg-primary-500/10 border border-primary-500/20 text-primary-400 text-sm font-medium backdrop-blur-sm"
      >
        <span class="relative flex h-2 w-2 mr-2">
          <span
            class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary-400 opacity-75"
          ></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-primary-500"
          ></span>
        </span>
        Available for new projects
      </div>

      <h1
        class="animate-fade-in-up font-display text-4xl md:text-6xl lg:text-7xl font-bold tracking-tight text-white leading-tight"
        style="animation-delay: 0.1s;"
      >
        Web Developer
        <span
          class="bg-gradient-to-r from-primary-400 via-accent-400 to-primary-500 bg-clip-text text-transparent animate-glow"
        >
          Building Modern Solutions
        </span>
        <span
          class="block text-xl md:text-2xl lg:text-3xl font-normal mt-4 text-neutral-300"
        >
          Based in St. Louis, MO • Serving Clients Worldwide
        </span>
      </h1>

      <p
        class="animate-fade-in-up text-lg md:text-xl text-neutral-300 max-w-2xl mx-auto lg:mx-0 leading-relaxed"
        style="animation-delay: 0.2s;"
      >
        I'm <strong class="text-white">Jake Meyers</strong>, a full-stack
        developer who builds websites and web applications that work. Need
        something intelligent? I can integrate AI features too. Let's create
        something that solves real problems.
      </p>

      <!-- CTA Buttons -->
      <div
        class="animate-fade-in-up flex flex-col sm:flex-row gap-4 justify-center lg:justify-start"
        style="animation-delay: 0.3s;"
      >
        <a
          href="#contact"
          class="group inline-flex items-center justify-center px-8 py-4 rounded-xl bg-gradient-to-r from-primary-500 to-accent-500 text-white font-semibold hover:from-primary-600 hover:to-accent-600 transition-all duration-300 shadow-glow-primary hover:shadow-glow-accent hover:scale-105"
        >
          Get in Touch
          <svg
            class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform duration-200"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>

        <a
          href="#portfolio"
          class="group inline-flex items-center justify-center px-8 py-4 rounded-xl bg-white/10 backdrop-blur-sm text-white font-semibold hover:bg-white/20 transition-all duration-300 border border-white/20 hover:border-white/30"
        >
          View My Work
          <svg
            class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform duration-200"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Stats -->
      <div
        class="animate-fade-in-up grid grid-cols-3 gap-8 pt-8"
        style="animation-delay: 0.4s;"
      >
        <div class="text-center lg:text-left">
          <div class="text-2xl md:text-3xl font-bold text-white">5+</div>
          <div class="text-sm text-neutral-400">Years Experience</div>
        </div>
        <div class="text-center lg:text-left">
          <div class="text-2xl md:text-3xl font-bold text-white">15+</div>
          <div class="text-sm text-neutral-400">Projects Delivered</div>
        </div>
        <div class="text-center lg:text-left">
          <div class="text-2xl md:text-3xl font-bold text-white">100%</div>
          <div class="text-sm text-neutral-400">Professional Quality</div>
        </div>
      </div>
    </div>

    <!-- Right Column - Modern Visual -->
    <div
      class="flex-1 relative w-full max-w-xl animate-fade-in-up"
      style="animation-delay: 0.5s;"
    >
      <!-- Floating Cards -->
      <div class="relative">
        <!-- Main Card - Code Editor Style -->
        <div
          class="relative bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-xl rounded-2xl border border-white/10 p-8 shadow-hard animate-float"
        >
          <div class="flex items-center justify-between mb-6">
            <div class="flex items-center space-x-3">
              <div class="w-3 h-3 rounded-full bg-success-500"></div>
              <div class="w-3 h-3 rounded-full bg-warning-500"></div>
              <div class="w-3 h-3 rounded-full bg-red-500"></div>
            </div>
            <div class="text-neutral-400 text-sm">app.jsx</div>
          </div>

          <div class="space-y-3 text-sm font-mono">
            <div class="text-accent-400">
              const <span class="text-white">project</span> = {}
              <div class="pl-4 text-neutral-300">
                type: <span class="text-success-400">'full-stack'</span>,
              </div>
              <div class="pl-4 text-neutral-300">
                stack: [<span class="text-primary-400">'React'</span>, <span
                  class="text-primary-400">'Node'</span
                >],
              </div>
              <div class="pl-4 text-neutral-300">
                features: <span class="text-accent-400">'modern'</span>,
              </div>
              <div class="pl-4 text-neutral-300">
                status: <span class="text-success-400">'deployed'</span>
              </div>
              <div class="text-accent-400"></div>
            </div>
          </div>
        </div>

        <!-- Floating Tech Icons -->
        <div
          class="absolute -top-4 -left-4 bg-blue-500 rounded-xl p-3 shadow-glow-primary animate-float"
          style="animation-delay: -1s;"
        >
          <svg
            class="w-6 h-6 text-white"
            fill="currentColor"
            viewBox="0 0 24 24"
          >
            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"
            ></path>
          </svg>
        </div>

        <div
          class="absolute -bottom-4 -right-4 bg-green-500 rounded-xl p-3 shadow-glow-accent animate-float"
          style="animation-delay: -2s;"
        >
          <svg
            class="w-6 h-6 text-white"
            fill="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"
            ></path>
          </svg>
        </div>

        <div
          class="absolute top-1/2 -right-8 bg-neutral-700 rounded-xl p-3 shadow-medium animate-float"
          style="animation-delay: -0.5s;"
        >
          <svg
            class="w-6 h-6 text-primary-400"
            fill="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              d="M9.4 16.6L4.8 12l4.6-4.6L8 6l-6 6 6 6 1.4-1.4zm5.2 0L19.2 12l-4.6-4.6L16 6l6 6-6 6-1.4-1.4z"
            ></path>
          </svg>
        </div>

        <!-- Subtle gradient orb behind card -->
        <div
          class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-primary-500/20 rounded-full blur-3xl -z-10"
        >
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom decoration -->
  <div
    class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gray-300 dark:via-gray-700 to-transparent"
  >
  </div>
</div>
